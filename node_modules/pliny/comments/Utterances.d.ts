interface UtterancesConfig {
    provider: 'utterances';
    utterancesConfig: {
        theme?: string;
        darkTheme?: string;
        repo?: string;
        label?: string;
        issueTerm?: string;
    };
}
declare type UtterancesProps = UtterancesConfig['utterancesConfig'];
declare const Utterances: ({ theme, darkTheme, repo, label, issueTerm }: UtterancesProps) => JSX.Element;

export { Utterances, UtterancesConfig, UtterancesProps };
