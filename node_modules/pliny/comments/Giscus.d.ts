interface GiscusConfig {
    provider: 'giscus';
    giscusConfig: {
        themeURL?: string;
        theme?: string;
        darkTheme?: string;
        mapping?: string;
        repo?: string;
        repositoryId?: string;
        category?: string;
        categoryId?: string;
        reactions?: string;
        metadata?: string;
        inputPosition?: string;
        lang?: string;
    };
}
declare type GiscusProps = GiscusConfig['giscusConfig'];
declare const Giscus: ({ themeURL, theme, darkTheme, repo, repositoryId, category, categoryId, reactions, metadata, inputPosition, lang, mapping, }: GiscusProps) => JSX.Element;

export { Giscus, GiscusConfig, GiscusProps };
