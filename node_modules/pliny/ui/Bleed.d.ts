import { ReactNode } from 'react';

interface BleedProps {
    full?: boolean;
    children: ReactNode;
}
declare const Bleed: ({ full, children }: BleedProps) => JSX.Element;

export { Bleed, BleedProps };
